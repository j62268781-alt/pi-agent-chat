# E2E 测试 harness — M1 骨架 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `pnpm test:e2e` 能启动一个隔离的 VS Code 实例、加载真实构建产物、跑起真实 pi 子进程，并用结构化断言证明 harness 骨架成立。

**Architecture:** `@vscode/test-electron` 的 `runTests()` 启动一个带独立 `--user-data-dir` / `--extensions-dir` 的 VS Code，`extensionDevelopmentPath` 指向仓库根加载 `dist/extension.cjs`。外层 runner 与内层 mocha 套件全部用 rolldown 打成 CJS（VS Code 扩展宿主用 `require` 加载 `extensionTestsPath`）。pi 会话目录通过 fixture `settings.json` 里的 `pi-agent-chat.env` 注入 `PI_CODING_AGENT_SESSION_DIR` 重定向到临时目录。

**Tech Stack:** `@vscode/test-electron@3.1.0`、`mocha@12`、`rolldown@1.0.0-rc.10`（已是 devDep）、`vitest@4`（已有）、真实 pi `0.85.1`。

**Spec:** `docs/superpowers/specs/2026-09-18-vscode-e2e-test-harness-design.md`

**范围说明：** 本计划只覆盖 M1。M2–M4 的设计细节依赖 M1 必须产出的 4 项结论（见 Task 5），故不在此计划内。

## Global Constraints

- 本地手动运行，**不进 CI**；不改 `.github/workflows/*`
- 真 pi，模型 pin **`solar/qoder/qwen3.8-flash`**（`solar/qoder/qwen3.8-max` 实测 404，账号组未开权限）
- **只断言结构，不断言文本** —— 任何用例都不得断言模型回复的具体内容
- VS Code 可执行文件：优先 `PI_E2E_VSCODE` 环境变量，其次 `/Applications/Visual Studio Code.app/Contents/MacOS/Electron`（实测 1.138.0 / stable / arm64），都不可用时省略 `vscodeExecutablePath` 让 test-electron 自行下载到 `.vscode-test/`
- **不使用** `reuseMachineInstall` —— 本机 `code` CLI 不在 PATH 上，其解析启发式不可靠
- **M1 不改任何生产代码**：`src/**`、`webview-vue/**`、`pi-extensions/**` 一律只读
- 时序预算：hydration 等待 30s，单个真 pi turn 120s（实测一句 `OK` 需 20.4s）
- 遵循房屋测试风格：不用 mock（零 `vi.mock`/`vi.fn`）、用真实原语、能力探测 + `skipIf` 而非假设、tracked 资源数组 + `afterEach` 清理、导入用显式 `.ts` 后缀、从 `"vitest"` 具名导入且不启用 globals
- **所有 git commit 必须先征得用户同意**（用户已明确规格书不提交）

## 执行环境实况（执行期查得，与规格书假设不同）

- **本机没有 pnpm**：`~/.zshenv:11` 把 `$HOME/.workbuddy/binaries/node/shims` 加进 PATH，注释说那是 pnpm/pnpx 的 corepack shims，但 `~/.workbuddy/binaries/node` 整个目录不存在；`~/Library/pnpm` 只有 `store`；`corepack` 也不在。交互式 zsh 里 `command -v pnpm` 同样为空。
- 因此**本地一律用 `npm run <script>`**，计划里出现的 `pnpm run ...` 命令均按此替换。但 `package.json` 里**提交**的 script 仍沿用仓库既有的 `pnpm run` / `pnpm --filter` 写法（`build`、`test`、`typecheck`、`package` 都是），因为 CI 有真 pnpm，且 e2e 不进 CI。
- 后果：本地 `npm run build` / `npm test` / `npm run typecheck` 会失败（它们内部调 `pnpm`），这是**既有状态**，不是本计划引入的。`npm run lint`、`npm run test:unit`、`npm run build:extension`、`npm run build:e2e` 不含 `pnpm`，可正常运行。
- **`node_modules` 与 `webview-vue/node_modules` 均为 root 所有**（349 / 432 个条目），当前用户 `<user>` 无写权限 → 任何安装都需要 sudo。`/usr/local/lib/node_modules` 也是 root:wheel，故 `npm i -g` 同样需要 sudo。`~/.local/bin` 是 `<user>` 的且在 PATH 上。
- 装依赖的既定路线（用户已选）：`npm install -D ...` 保持 node_modules 的 npm 布局，再 `npx --yes pnpm@10.32.1 install --lockfile-only` 只更新 `pnpm-lock.yaml`，避免 CI 的 `--frozen-lockfile` 失败。执行前已核实 `pnpm-lock.yaml` 与 `package.json` 完全同步（双向零缺失、零 specifier 不匹配，lockfileVersion 9.0）。
- `@vscode/test-electron@3.1.0` 的 `scripts` 里没有 `install`/`postinstall`，安装期不下载 VS Code，故 `pnpm-workspace.yaml` 的 `onlyBuiltDependencies: [esbuild]` 不构成阻碍。

---

## File Structure

```
test/e2e/
  rolldown.config.ts        # 新建：三个入口 → CJS，输出到 .build/
  tsconfig.json             # 新建：CJS 语义下 typecheck e2e 的 TS
  fixture.ts                # 新建：路径解析、settings 生成、临时目录生命周期、pi 进程匹配
  fixture.test.ts           # 新建：fixture.ts 的 vitest 单测（进 pnpm test:unit）
  runner.ts                 # 新建：外层进程 —— build → fixture → runTests → 孤儿检查 → 清理
  FINDINGS.md               # 新建：M1 四项不确定项的书面结论
  .build/                   # 产物，gitignore
    runner.cjs
    suite/index.cjs
    suite/cases/m1-bootstrap.cjs
    suite/chunks/*.cjs
  suite/
    index.ts                # 新建：mocha 入口，导出 run()
    harness.ts              # 新建：宿主内 helper（env 读取、waitFor、目录遍历）
    cases/
      m1-bootstrap.test.ts  # 新建：M1 用例

vitest.config.ts            # 新建：把 test/e2e/suite 与 .build 排除出 vitest
package.json                # 修改：devDeps + build:e2e / test:e2e script + typecheck 串联
.gitignore                  # 修改：test/e2e/.build/、.vscode-test/
.oxlintrc.json              # 修改：ignorePatterns 加 test/e2e/.build
.oxfmtrc.json               # 修改：ignorePatterns 加 test/e2e/.build
README.md                   # 修改：Development 段落加 e2e 说明
```

**职责边界：** `fixture.ts` 是唯一知道「路径与设置长什么样」的地方，被外层 runner、内层套件、以及 vitest 单测三方共用（打进各自 bundle，无共享状态）。`harness.ts` 只在扩展宿主内运行，只依赖 `process.env` 与 `vscode`。`runner.ts` 只做编排，不含断言逻辑。

---

### Task 1: 依赖与配置地基

**Files:**

- Modify: `package.json`（devDependencies、scripts）
- Create: `vitest.config.ts`
- Modify: `.gitignore`
- Modify: `.oxlintrc.json`
- Modify: `.oxfmtrc.json`

**Interfaces:**

- Consumes: 无
- Produces: npm scripts `build:e2e`、`test:e2e`；devDeps `@vscode/test-electron`、`mocha`、`@types/mocha`；vitest 对 `test/e2e/suite/**` 与 `test/e2e/.build/**` 的排除

- [ ] **Step 1: 装依赖**

Run:

```bash
pnpm add -D @vscode/test-electron@^3.1.0 mocha@^12.0.2 @types/mocha@^10.0.10
```

Expected: 成功，且**不出现任何 VS Code 下载进度**。

- [ ] **Step 2: 确认没有 postinstall 下载被 pnpm 白名单拦掉**

`@vscode/test-electron@3.1.0` 的 `scripts` 里没有 `install`/`postinstall`（已核实），所以 `pnpm-workspace.yaml` 的 `onlyBuiltDependencies: [esbuild]` 不会拦它。验证：

Run:

```bash
node -e 'console.log(Object.keys(require("./node_modules/@vscode/test-electron/package.json").scripts))'
ls node_modules/@vscode/test-electron/out/runTest.js
```

Expected: 打印的 scripts 数组里不含 `install` / `postinstall`；`runTest.js` 存在。

> 这条把规格书 §4 M1 的第 5 项不确定项（pnpm 白名单）**关掉**。结论要抄进 Task 5 的 FINDINGS.md。

- [ ] **Step 3: 建 `vitest.config.ts`**

必须新建：仓库当前没有任何 vitest 配置，`pnpm test:unit` 是裸 `vitest run`，默认 include 是 `**/*.{test,spec}.?(c|m)[jt]s?(x)`。若不排除，Task 4 的 `m1-bootstrap.test.ts` 会被 vitest 当单测跑并因缺少 `vscode` API 而炸。

```ts
import { configDefaults, defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    exclude: [
      // configDefaults.exclude 在 vitest 4 里只有 node_modules 与 .git，
      // 必须 spread 保留再追加，否则会把默认排除项覆盖掉。
      ...configDefaults.exclude,
      // e2e 套件需要真实的 VS Code 扩展宿主与 `vscode` API，
      // 由 @vscode/test-electron 通过 `pnpm test:e2e` 驱动，不走 vitest。
      // 注意：test/e2e/fixture.test.ts 是普通单测，故意不在排除范围内。
      "test/e2e/suite/**",
      "test/e2e/.build/**",
    ],
  },
});
```

- [ ] **Step 4: 改 `.gitignore`**

在文件末尾追加：

```
# e2e harness build output
test/e2e/.build/

# VS Code copies downloaded by @vscode/test-electron (only when
# PI_E2E_VSCODE is unset and no local install is found)
.vscode-test/
```

- [ ] **Step 5: 改 `.oxlintrc.json` 与 `.oxfmtrc.json`**

两个文件的 `ignorePatterns` 都追加 `"test/e2e/.build"`（打包产物不是源码，不该被 lint/format）：

```json
"ignorePatterns": ["webview-vue/src/lib/model-icons-data.ts", "webview-vue/dist", "dist", "test/e2e/.build"]
```

- [ ] **Step 6: 改 `package.json` scripts**

在 `scripts` 里新增两条，并扩展 `typecheck`：

```jsonc
"build:e2e": "rolldown -c test/e2e/rolldown.config.ts",
"test:e2e": "pnpm run build:e2e && node test/e2e/.build/runner.cjs",
"typecheck": "tsgo --noEmit --skipLibCheck && tsgo --noEmit -p test/e2e/tsconfig.json && node scripts/typecheck-pi-extensions.mjs && pnpm --filter @pi-agent-chat/webview-vue typecheck"
```

`typecheck` 原值（保持其余部分不变，只在第一段后插入 e2e 那段）：

```
tsgo --noEmit --skipLibCheck && node scripts/typecheck-pi-extensions.mjs && pnpm --filter @pi-agent-chat/webview-vue typecheck
```

**不要**把 `test:e2e` 串进 `pnpm test`（那是 CI 用的，e2e 不进 CI）。

- [ ] **Step 7: 验证既有测试与 lint 未被破坏**

Run:

```bash
npm run lint
npm run test:unit
```

Expected:

- `lint`：`Found 0 warnings and 0 errors` 且 `All matched files use the correct format.`
- `test:unit`：**`endpoint.test.ts` 全绿；`bind.test.ts` 在 macOS 上有 2 个既有失败**，与本计划无关：
  - `bindServer TCP > falls back to a random port when the fixed port is in use`（`expected undefined to be <port>`）
  - `unlinkStaleSocket > unlinks a stale socket file when nothing is listening`（`expected true to be false`）

  两者都是 macOS 与 Linux 的行为差异：前者是端口回退未记录，后者是该用例用普通文件伪造「陈旧 socket」（测试注释自陈 "on real Linux this is what a closed server leaves behind"），而 macOS 对普通文件 `connect()` 的 errno 与 Linux 不同，故未触发 unlink。CI 跑 `ubuntu-latest`，在 CI 上是绿的。已验证与本计划无关：把 `vitest.config.ts` 移开后同样失败（2 failed | 9 passed）。

  **判定标准：失败必须恰好是这 2 个、且是这 2 个名字。** 出现任何其他失败即为本次改动引入，须停下排查。

- 此时 `test:e2e` 还不能跑（rolldown 配置在 Task 3 才建），这是预期的。

- [ ] **Step 8: 提交（先问用户）**

```bash
git add package.json pnpm-lock.yaml vitest.config.ts .gitignore .oxlintrc.json .oxfmtrc.json
git commit -m "test(e2e): groundwork for the VS Code e2e harness"
```

---

### Task 2: fixture 模块与其单测

**Files:**

- Create: `test/e2e/fixture.ts`
- Create: `test/e2e/fixture.test.ts`

**Interfaces:**

- Consumes: 无
- Produces（Task 3/4 依赖，签名必须完全一致）:
  - `const E2E_MODEL = "solar/qoder/qwen3.8-flash"`
  - `const EXTENSION_ID = "johnny-zhao.pi-agent-chat"`
  - `resolvePiBinary(env?: NodeJS.ProcessEnv): string`
  - `resolveVsCodeExecutablePath(env?: NodeJS.ProcessEnv): string | undefined`
  - `fixtureSettings(opts: { piBin: string; sessionsDir: string; offline: boolean }): Record<string, unknown>`
  - `createFixture(opts: { repoRoot: string; offline?: boolean }): Fixture`
  - `disposeFixture(root: string): void`
  - `runningCommandLines(): string[]`
  - `findTestPiProcesses(lines?: string[]): string[]`
  - `interface Fixture { root: string; workspace: string; userDataDir: string; extensionsDir: string; sessionsDir: string; piBin: string }`

- [ ] **Step 1: 写失败的单测 `test/e2e/fixture.test.ts`**

遵循房屋风格：具名导入、真实原语、tracked 资源 + `afterEach` 清理、每处 skip 有注释说明原因。

```ts
import { existsSync, readFileSync, rmSync } from "node:fs";
import { join } from "node:path";
import { afterEach, describe, expect, it } from "vitest";
import {
  createFixture,
  disposeFixture,
  E2E_MODEL,
  findTestPiProcesses,
  fixtureSettings,
  resolvePiBinary,
  resolveVsCodeExecutablePath,
} from "./fixture.ts";

const created: string[] = [];

function newFixture(offline = true) {
  const fixture = createFixture({ repoRoot: process.cwd(), offline });
  created.push(fixture.root);
  return fixture;
}

afterEach(() => {
  while (created.length > 0) {
    const root = created.pop();
    if (root) rmSync(root, { recursive: true, force: true });
  }
});

describe("fixtureSettings", () => {
  it("pins the pi binary and model before activation", () => {
    const settings = fixtureSettings({
      piBin: "/tmp/pi",
      sessionsDir: "/tmp/sessions",
      offline: true,
    });

    // 二进制路径有缓存，只在 pi-agent-chat.path 的配置变更事件里失效
    // (src/extension.ts:83)，所以必须在 fixture settings 里一次写死。
    expect(settings["pi-agent-chat.path"]).toBe("/tmp/pi");
    expect(settings["pi-agent-chat.args"]).toEqual(["--model", E2E_MODEL]);
    expect(settings["pi-agent-chat.env"]).toEqual({
      PI_CODING_AGENT_SESSION_DIR: "/tmp/sessions",
      PI_OFFLINE: "1",
    });
    expect(settings["pi-agent-chat.ui"]).toBe("sidebar");
  });

  it("omits PI_OFFLINE when offline is false", () => {
    const settings = fixtureSettings({
      piBin: "/tmp/pi",
      sessionsDir: "/tmp/sessions",
      offline: false,
    });
    expect(settings["pi-agent-chat.env"]).toEqual({
      PI_CODING_AGENT_SESSION_DIR: "/tmp/sessions",
    });
  });
});

describe("resolveVsCodeExecutablePath", () => {
  it("prefers PI_E2E_VSCODE over everything", () => {
    expect(resolveVsCodeExecutablePath({ PI_E2E_VSCODE: "/custom/Electron" })).toBe(
      "/custom/Electron",
    );
  });

  // 只有 darwin 上才存在这个固定路径，别的平台跳过而不是假设。
  it.skipIf(process.platform !== "darwin")("falls back to the machine install", () => {
    const resolved = resolveVsCodeExecutablePath({});
    if (!existsSync("/Applications/Visual Studio Code.app/Contents/MacOS/Electron")) {
      expect(resolved).toBeUndefined();
      return;
    }
    expect(resolved).toBe("/Applications/Visual Studio Code.app/Contents/MacOS/Electron");
  });

  it("returns undefined when nothing is found so test-electron downloads", () => {
    // 非 darwin 且没有 PI_E2E_VSCODE 时必须是 undefined，
    // 这是「回落到下载」的触发条件。
    if (process.platform === "darwin") return;
    expect(resolveVsCodeExecutablePath({})).toBeUndefined();
  });
});

describe("resolvePiBinary", () => {
  it("honours PI_E2E_PI", () => {
    expect(resolvePiBinary({ PI_E2E_PI: "/custom/pi" })).toBe("/custom/pi");
  });
});

describe("findTestPiProcesses", () => {
  const match = `/usr/bin/pi --mode rpc --model ${E2E_MODEL}`;

  it("matches only processes carrying both the rpc mode and our model", () => {
    expect(
      findTestPiProcesses([
        match,
        "/usr/bin/pi --mode rpc --model anthropic/claude-sonnet",
        "/usr/bin/node --mode rpc",
        "grep --color=auto pi",
      ]),
    ).toEqual([match]);
  });

  it("returns an empty array when nothing matches", () => {
    expect(findTestPiProcesses(["/sbin/launchd"])).toEqual([]);
  });
});

describe("createFixture", () => {
  it("creates every directory and writes the workspace settings", () => {
    const fixture = newFixture();

    for (const dir of [
      fixture.workspace,
      fixture.userDataDir,
      fixture.extensionsDir,
      fixture.sessionsDir,
    ]) {
      expect(existsSync(dir)).toBe(true);
    }

    const settingsPath = join(fixture.workspace, ".vscode", "settings.json");
    expect(existsSync(settingsPath)).toBe(true);

    const written = JSON.parse(readFileSync(settingsPath, "utf8")) as Record<string, unknown>;
    expect(written["pi-agent-chat.args"]).toEqual(["--model", E2E_MODEL]);
    expect(written["pi-agent-chat.env"]).toMatchObject({
      PI_CODING_AGENT_SESSION_DIR: fixture.sessionsDir,
    });
  });

  it("nests every path under a single removable root", () => {
    const fixture = newFixture();
    for (const dir of [
      fixture.workspace,
      fixture.userDataDir,
      fixture.extensionsDir,
      fixture.sessionsDir,
    ]) {
      expect(dir.startsWith(fixture.root)).toBe(true);
    }
    disposeFixture(fixture.root);
    expect(existsSync(fixture.root)).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm exec vitest run test/e2e/fixture.test.ts`
Expected: FAIL，报 `Cannot find module './fixture.ts'`（或等价的解析错误）。

- [ ] **Step 3: 写 `test/e2e/fixture.ts`**

```ts
// Everything the e2e harness needs to know about paths and pinned settings.
//
// Shared by three consumers that never run in the same process: the outer
// runner (`runner.ts`), the in-host mocha suite (`suite/cases/*`), and the
// vitest unit tests (`fixture.test.ts`). Each gets its own inlined copy via
// rolldown, so this module must stay free of module-level mutable state.

import { execFileSync } from "node:child_process";
import { existsSync, mkdirSync, mkdtempSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

/**
 * Pinned for every e2e run. Verified against the solar provider:
 * `qoder/qwen3.8-max` returns 404 model_not_found for this account group,
 * while `qoder/qwen3.8-flash`, `qoder/glm-5.3-flash` and `deepseek-v4.1-flash`
 * all answer. A trivial turn on the flash model measured 20.4s.
 */
export const E2E_MODEL = "solar/qoder/qwen3.8-flash";

/** `${publisher}.${name}` from package.json; the M1 suite asserts this matches. */
export const EXTENSION_ID = "johnny-zhao.pi-agent-chat";

/** macOS install verified at 1.138.0 / stable / arm64. */
const DARWIN_VSCODE = "/Applications/Visual Studio Code.app/Contents/MacOS/Electron";

export interface Fixture {
  root: string;
  workspace: string;
  userDataDir: string;
  extensionsDir: string;
  sessionsDir: string;
  piBin: string;
}

export function resolvePiBinary(env: NodeJS.ProcessEnv = process.env): string {
  if (env.PI_E2E_PI) return env.PI_E2E_PI;
  try {
    const resolved = execFileSync("which", ["pi"], { encoding: "utf8" }).trim();
    if (resolved) return resolved;
  } catch {
    // `which` exits non-zero when pi is not on PATH; fall through to the
    // documented probe order in src/services/pi/binary.ts:60.
  }
  const fallback = join(env.HOME ?? "", ".local/bin/pi");
  if (existsSync(fallback)) return fallback;
  throw new Error("cannot locate the pi binary; set PI_E2E_PI to an absolute path");
}

/**
 * Deliberately does not use @vscode/test-electron's `reuseMachineInstall`:
 * the `code` CLI is not on PATH on this machine, so its resolution heuristics
 * are unreliable. Returning undefined makes test-electron download its own
 * copy into `defaultCachePath` (.vscode-test/) instead.
 */
export function resolveVsCodeExecutablePath(
  env: NodeJS.ProcessEnv = process.env,
): string | undefined {
  if (env.PI_E2E_VSCODE) return env.PI_E2E_VSCODE;
  if (process.platform === "darwin" && existsSync(DARWIN_VSCODE)) return DARWIN_VSCODE;
  return undefined;
}

/**
 * Settings for the fixture workspace. `path` and `args` must be present before
 * activation: the pi binary resolution is cached and only invalidated on a
 * `pi-agent-chat.path` config-change event (src/extension.ts:83).
 *
 * `PI_CODING_AGENT_SESSION_DIR` rides in through `pi-agent-chat.env`, which
 * `createRpcEnvironment` (src/services/rpc/process.ts:158-166) merges into the
 * subprocess env. It is not one of the extension-injected PI_VSCODE_* vars, so
 * it cannot be overwritten.
 */
export function fixtureSettings(opts: {
  piBin: string;
  sessionsDir: string;
  offline: boolean;
}): Record<string, unknown> {
  return {
    "pi-agent-chat.path": opts.piBin,
    "pi-agent-chat.ui": "sidebar",
    "pi-agent-chat.args": ["--model", E2E_MODEL],
    "pi-agent-chat.env": {
      PI_CODING_AGENT_SESSION_DIR: opts.sessionsDir,
      ...(opts.offline ? { PI_OFFLINE: "1" } : {}),
    },
    "pi-agent-chat.rpcTrace": true,
    // Keep the fixture window quiet and deterministic.
    "telemetry.telemetryLevel": "off",
    "workbench.startupEditor": "none",
    "workbench.enableExperiments": false,
    "extensions.autoUpdate": false,
    "extensions.autoCheckUpdates": false,
  };
}

export function createFixture(opts: { repoRoot: string; offline?: boolean }): Fixture {
  const root = mkdtempSync(join(tmpdir(), "pi-e2e-"));
  const fixture: Fixture = {
    root,
    workspace: join(root, "workspace"),
    userDataDir: join(root, "user-data"),
    extensionsDir: join(root, "extensions"),
    sessionsDir: join(root, "pi-sessions"),
    piBin: resolvePiBinary(),
  };

  for (const dir of [
    fixture.workspace,
    fixture.userDataDir,
    fixture.extensionsDir,
    fixture.sessionsDir,
  ]) {
    mkdirSync(dir, { recursive: true });
  }

  mkdirSync(join(fixture.workspace, ".vscode"), { recursive: true });
  writeFileSync(
    join(fixture.workspace, ".vscode", "settings.json"),
    `${JSON.stringify(
      fixtureSettings({
        piBin: fixture.piBin,
        sessionsDir: fixture.sessionsDir,
        offline: opts.offline ?? true,
      }),
      null,
      2,
    )}\n`,
  );
  writeFileSync(join(fixture.workspace, "sample.md"), "# pi-agent-chat e2e fixture\n");

  return fixture;
}

export function disposeFixture(root: string): void {
  rmSync(root, { recursive: true, force: true });
}

/**
 * Full command lines of every running process. `-ww` matters: without it macOS
 * truncates to the terminal width, and `--model` sits at the very end of pi's
 * argument list. Measured longest line on this machine: 3151 chars.
 */
export function runningCommandLines(): string[] {
  return execFileSync("ps", ["-Awwo", "command="], {
    encoding: "utf8",
    maxBuffer: 64 * 1024 * 1024,
  })
    .split("\n")
    .filter((line) => line.length > 0);
}

/**
 * `--model solar/qoder/qwen3.8-flash` is injected through `pi-agent-chat.args`,
 * so it is unique to this fixture. Matching on it proves three things at once:
 * pi spawned, the fixture args reached the subprocess, and the process belongs
 * to the test rather than to a VS Code the user has open by hand.
 */
export function findTestPiProcesses(lines: string[] = runningCommandLines()): string[] {
  return lines.filter(
    (line) => line.includes("--mode rpc") && line.includes(`--model ${E2E_MODEL}`),
  );
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm exec vitest run test/e2e/fixture.test.ts`
Expected: PASS，全部用例绿。

- [ ] **Step 5: 确认没被主 typecheck 漏掉也没把它搞坏**

此时 `test/e2e/tsconfig.json` 还没建（Task 3），所以先只跑 lint 与全量单测：

Run: `pnpm run lint && pnpm run test:unit`
Expected: 全绿；`test:unit` 现在应报告 3 个测试文件（原有 2 个 + `fixture.test.ts`）。

- [ ] **Step 6: 提交（先问用户）**

```bash
git add test/e2e/fixture.ts test/e2e/fixture.test.ts
git commit -m "test(e2e): fixture module for workspace, pi and VS Code resolution"
```

---

### Task 3: 套件打包与宿主内 helper

**Files:**

- Create: `test/e2e/rolldown.config.ts`
- Create: `test/e2e/tsconfig.json`
- Create: `test/e2e/suite/harness.ts`
- Create: `test/e2e/suite/index.ts`
- Create: `test/e2e/suite/cases/m1-bootstrap.test.ts`（本 Task 只放占位用例，真实用例在 Task 4 补齐）

**Interfaces:**

- Consumes: `E2E_MODEL`、`EXTENSION_ID`、`findTestPiProcesses`（来自 Task 2 的 `fixture.ts`）
- Produces（Task 4 依赖）:
  - `env(name: string): string`（缺失时抛出带诊断信息的错误）
  - `repoRoot(): string`、`workspaceDir(): string`、`sessionsDir(): string`
  - `waitFor(predicate: () => boolean | Promise<boolean>, opts: { timeoutMs: number; intervalMs?: number; what?: string }): Promise<void>`
  - `filesUnder(dir: string): string[]`
  - `suite/index.ts` 的 `export async function run(): Promise<void>`
  - 构建产物路径 `test/e2e/.build/suite/index.cjs`

- [ ] **Step 1: 建 `test/e2e/rolldown.config.ts`**

风格对齐仓库根的 `rolldown.config.ts`（同样是 `format: "cjs"`、`platform: "node"`、`chunkFileNames` 提取共享 chunk）。两处刻意不同：**不 minify**（失败诊断比体积重要），**多入口**（mocha 需要能用 `addFile` 逐个加载用例文件）。

```ts
import { defineConfig } from "rolldown";

/**
 * The e2e suite is bundled to CommonJS because VS Code's extension host
 * `require()`s whatever `extensionTestsPath` points at, and mocha's `addFile`
 * needs each case to be a separate on-disk module.
 *
 * Shared modules (fixture.ts, harness.ts) are extracted into `chunks/` by
 * `chunkFileNames`, the same way the extension build does it — so the runner
 * and the cases all see one copy at runtime.
 *
 * Deliberately not minified: when a case fails, the stack trace is the main
 * diagnostic we get.
 */
export default defineConfig({
  input: {
    runner: "test/e2e/runner.ts",
    "suite/index": "test/e2e/suite/index.ts",
    "suite/cases/m1-bootstrap": "test/e2e/suite/cases/m1-bootstrap.test.ts",
  },
  // `vscode` is injected by the extension host at runtime; `mocha` and
  // `@vscode/test-electron` resolve from the repo's node_modules at runtime.
  external: ["vscode", "mocha", "@vscode/test-electron"],
  platform: "node",
  output: {
    dir: "test/e2e/.build",
    cleanDir: true,
    entryFileNames: "[name].cjs",
    chunkFileNames: "chunks/[name]-[hash].cjs",
    format: "cjs",
    sourcemap: true,
    minify: false,
  },
});
```

- [ ] **Step 2: 建 `test/e2e/tsconfig.json`**

根 tsconfig 的 `include` 只有 `["src"]`，所以 e2e 的 TS 现在完全没被 typecheck。用 CJS 语义而非仓库的 nodenext ESM，因为产物就是 CJS —— 这样 `import Mocha from "mocha"`（`export =` 模块）不会有 ESM 互操作摩擦。

```jsonc
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // The suite is bundled to CommonJS (the extension host `require()`s
    // `extensionTestsPath`), so typecheck it under CJS semantics instead of the
    // repo's nodenext ESM. This also lets `import Mocha from "mocha"` work
    // against mocha's `export =` typings without ESM interop friction.
    "module": "commonjs",
    "moduleResolution": "node",
    "verbatimModuleSyntax": false,
    "types": ["node", "vscode", "mocha"],
  },
  "include": ["."],
}
```

- [ ] **Step 3: 建 `test/e2e/suite/harness.ts`**

```ts
// Helpers that only make sense inside the VS Code extension host. Kept free of
// fixture knowledge so the cases read as assertions, not plumbing.

import { existsSync, readdirSync } from "node:fs";
import { join } from "node:path";

/**
 * Fixture paths reach the extension host through `extensionTestsEnv`, which
 * @vscode/test-electron applies via `cp.spawn`'s env (out/runTest.js). A
 * missing key therefore means the env did not propagate at all — say so loudly
 * instead of letting a later assertion fail on `undefined`.
 */
export function env(name: string): string {
  const value = process.env[name];
  if (value) return value;
  const known = Object.keys(process.env)
    .filter((key) => key.startsWith("PI_E2E_"))
    .join(", ");
  throw new Error(
    `${name} is missing from the extension host environment; extensionTestsEnv did ` +
      `not propagate. PI_E2E_* keys seen: ${known || "(none)"}`,
  );
}

export const repoRoot = (): string => env("PI_E2E_REPO");
export const workspaceDir = (): string => env("PI_E2E_WORKSPACE");
export const sessionsDir = (): string => env("PI_E2E_SESSIONS");

export async function waitFor(
  predicate: () => boolean | Promise<boolean>,
  opts: { timeoutMs: number; intervalMs?: number; what?: string },
): Promise<void> {
  const intervalMs = opts.intervalMs ?? 500;
  const deadline = Date.now() + opts.timeoutMs;
  for (;;) {
    if (await predicate()) return;
    if (Date.now() > deadline) {
      throw new Error(
        `timed out after ${opts.timeoutMs}ms waiting for ${opts.what ?? "condition"}`,
      );
    }
    await new Promise((resolve) => setTimeout(resolve, intervalMs));
  }
}

/**
 * Every file under `dir`, recursively. pi's session layout is not part of our
 * contract, so callers assert on "something landed", never on a filename.
 */
export function filesUnder(dir: string): string[] {
  if (!existsSync(dir)) return [];
  const found: string[] = [];
  for (const entry of readdirSync(dir, { withFileTypes: true })) {
    const full = join(dir, entry.name);
    if (entry.isDirectory()) found.push(...filesUnder(full));
    else found.push(full);
  }
  return found;
}
```

- [ ] **Step 4: 建 `test/e2e/suite/index.ts`**

VS Code 扩展宿主 `require()` 这个模块并 await 其 `run()` 导出。不用 `glob`：入口在打包期已知，`readdirSync` 足够，少一个依赖。

```ts
// Bundled to `test/e2e/.build/suite/index.cjs` and passed to
// @vscode/test-electron as `extensionTestsPath`. VS Code's extension host
// requires this module and awaits its `run()` export.
import { readdirSync } from "node:fs";
import { join } from "node:path";
import Mocha from "mocha";

/** A trivial real-pi turn measured 20.4s; leave room for tool calls. */
const SUITE_TIMEOUT_MS = 120_000;

export async function run(): Promise<void> {
  const mocha = new Mocha({ ui: "bdd", color: true, timeout: SUITE_TIMEOUT_MS });

  const casesDir = join(__dirname, "cases");
  const names = readdirSync(casesDir)
    .filter((name) => name.endsWith(".cjs"))
    .sort();
  for (const name of names) mocha.addFile(join(casesDir, name));
  mocha.loadFiles();

  const failures = await new Promise<number>((resolve) => {
    mocha.run((count) => resolve(count));
  });
  if (failures > 0) throw new Error(`${failures} e2e case(s) failed`);
}
```

- [ ] **Step 5: 建一个最小的 `test/e2e/suite/cases/m1-bootstrap.test.ts`**

本步只放一条能在宿主内跑通的最简断言，用来验证「打包 → 加载 → mocha 执行」这条链路。真实用例在 Task 4 替换进来。

```ts
import { strict as assert } from "node:assert";
import { env } from "../harness.ts";

describe("M1 — bundle wiring", () => {
  it("runs inside the extension host and sees the fixture env", () => {
    assert.ok(env("PI_E2E_REPO").length > 0);
  });
});
```

- [ ] **Step 6: 建一个最小的 `test/e2e/runner.ts` 占位**

Task 4 会替换成完整实现。此处只需让打包有一个可解析的入口：

```ts
// Placeholder — replaced in Task 4.
export {};
```

- [ ] **Step 7: 打包并验证产物结构**

Run: `pnpm run build:e2e`
Expected: 成功，且产出下列文件：

Run:

```bash
find test/e2e/.build -type f | sort
```

Expected 输出包含：

```
test/e2e/.build/runner.cjs
test/e2e/.build/suite/cases/m1-bootstrap.cjs
test/e2e/.build/suite/index.cjs
```

（另有若干 `test/e2e/.build/chunks/*.cjs` 与 `*.map`，以及 `runner.cjs.map` 等。`fixture.ts` / `harness.ts` 应出现在 chunks 里而不是被复制进每个入口。）

- [ ] **Step 8: 验证 `vscode` 与 `mocha` 确实被外部化**

Run:

```bash
grep -c 'require("vscode")' test/e2e/.build/suite/cases/m1-bootstrap.cjs
grep -c 'require("mocha")' test/e2e/.build/suite/index.cjs
```

Expected: 两个都 ≥ 1。若为 0，说明 `external` 没生效，mocha/vscode 被打进了 bundle —— 必须修掉再继续。

- [ ] **Step 9: typecheck**

Run: `pnpm exec tsgo --noEmit -p test/e2e/tsconfig.json`
Expected: 无错误。若报 `Cannot find name 'describe'`，说明 `types` 里的 `"mocha"` 没生效，检查 `test/e2e/tsconfig.json` 是否正确 extends。

- [ ] **Step 10: lint + 全量 typecheck**

Run: `pnpm run lint && pnpm run typecheck`
Expected: 全绿。`typecheck` 现在会多跑一段 `tsgo --noEmit -p test/e2e/tsconfig.json`（Task 1 Step 6 已串好）。

- [ ] **Step 11: 提交（先问用户）**

```bash
git add test/e2e/rolldown.config.ts test/e2e/tsconfig.json test/e2e/runner.ts test/e2e/suite
git commit -m "test(e2e): bundle the mocha suite to CJS for the extension host"
```

---

### Task 4: 外层 runner 与 M1 真实用例

**Files:**

- Modify: `test/e2e/runner.ts`（替换 Task 3 的占位）
- Modify: `test/e2e/suite/cases/m1-bootstrap.test.ts`（替换 Task 3 的最小用例）

**Interfaces:**

- Consumes: Task 2 的 `createFixture` / `disposeFixture` / `resolveVsCodeExecutablePath` / `findTestPiProcesses` / `E2E_MODEL` / `EXTENSION_ID`；Task 3 的 `env` / `waitFor` / `filesUnder` / `sessionsDir` / `repoRoot`；`@vscode/test-electron` 的 `runTests(options: TestOptions): Promise<number>`
- Produces: 可执行的 `pnpm test:e2e`

- [ ] **Step 1: 实现 `test/e2e/runner.ts`**

```ts
// Outer process: build, prepare an isolated fixture, launch VS Code, then
// verify nothing was left behind. All assertions about the extension itself
// live in `suite/cases/`; this file only orchestrates and checks the seams
// that are invisible from inside the host (orphan processes, temp dirs).
import { spawnSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";
import { join } from "node:path";
import { runTests } from "@vscode/test-electron";
import {
  createFixture,
  disposeFixture,
  findTestPiProcesses,
  resolveVsCodeExecutablePath,
  type Fixture,
} from "./fixture.ts";

/**
 * `process.cwd()` rather than `__dirname`: this module is bundled into
 * `.build/runner.cjs`, so source-relative paths would resolve against the
 * output directory. pnpm always runs scripts with cwd = package root, and the
 * guard below fails loudly if that ever stops being true.
 */
function resolveRepoRoot(): string {
  const root = process.cwd();
  const manifestPath = join(root, "package.json");
  if (!existsSync(manifestPath)) {
    throw new Error(`no package.json under cwd ${root}; run this through pnpm`);
  }
  const manifest = JSON.parse(readFileSync(manifestPath, "utf8")) as { name?: string };
  if (manifest.name !== "pi-agent-chat") {
    throw new Error(`cwd ${root} is not the pi-agent-chat repo root (name: ${manifest.name})`);
  }
  return root;
}

function build(repoRoot: string): number {
  // Hard ordering: the extension build inlines webview-vue/dist/**/index.html
  // with ?raw (src/providers/chat/webview-html.ts:4), so the webview must exist
  // first. `pnpm build` already runs build:webview before build:extension.
  const result = spawnSync("pnpm", ["run", "build"], { cwd: repoRoot, stdio: "inherit" });
  return result.status ?? 1;
}

async function main(): Promise<number> {
  const repoRoot = resolveRepoRoot();
  const argv = process.argv.slice(2);
  const offline = !argv.includes("--no-offline");

  if (!argv.includes("--skip-build")) {
    const status = build(repoRoot);
    if (status !== 0) return status;
  }

  const fixture: Fixture = createFixture({ repoRoot, offline });
  const vscodeExecutablePath = resolveVsCodeExecutablePath();

  console.log(`[e2e] fixture   ${fixture.root}`);
  console.log(`[e2e] pi        ${fixture.piBin}`);
  console.log(`[e2e] vscode    ${vscodeExecutablePath ?? "(download into .vscode-test/)"}`);
  console.log(`[e2e] offline   ${offline}`);

  let exitCode = 0;
  let failure: string | undefined;
  try {
    exitCode = await runTests({
      // Omitted entirely when undefined so test-electron falls back to its own
      // download; passing `undefined` explicitly would be equivalent but is
      // harder to read in the resolved options.
      ...(vscodeExecutablePath === undefined ? {} : { vscodeExecutablePath }),
      extensionDevelopmentPath: repoRoot,
      extensionTestsPath: join(repoRoot, "test/e2e/.build/suite/index.cjs"),
      launchArgs: [
        fixture.workspace,
        "--user-data-dir",
        fixture.userDataDir,
        "--extensions-dir",
        fixture.extensionsDir,
        "--disable-workspace-trust",
        "--skip-welcome",
        "--skip-release-notes",
        "--new-window",
      ],
      extensionTestsEnv: {
        PI_E2E_REPO: repoRoot,
        PI_E2E_WORKSPACE: fixture.workspace,
        PI_E2E_SESSIONS: fixture.sessionsDir,
        PI_E2E_PI_BIN: fixture.piBin,
        // Also injected through pi-agent-chat.env in the fixture settings.
        // Setting it here too means pi isolation survives a regression in the
        // settings path.
        PI_CODING_AGENT_SESSION_DIR: fixture.sessionsDir,
      },
    });
  } catch (error) {
    // @vscode/test-electron v3 throws TestRunFailedError on a non-zero exit.
    failure = error instanceof Error ? error.message : String(error);
  }

  const orphans = findTestPiProcesses();
  if (orphans.length > 0) {
    console.error(`[e2e] ${orphans.length} orphan pi process(es) survived teardown:`);
    for (const line of orphans) console.error(`[e2e]   ${line}`);
  }

  if (exitCode === 0 && failure === undefined && orphans.length === 0) {
    disposeFixture(fixture.root);
  } else {
    // Keep it: the user-data-dir and the rpcTrace output are the only way to
    // diagnose a launch that never got as far as reporting.
    console.error(`[e2e] fixture kept for inspection: ${fixture.root}`);
  }

  if (failure !== undefined) {
    console.error(`[e2e] run failed: ${failure}`);
    return 1;
  }
  return orphans.length > 0 ? 1 : exitCode;
}

main().then(
  (code) => process.exit(code),
  (error: unknown) => {
    console.error(error);
    process.exit(1);
  },
);
```

- [ ] **Step 2: 实现真实的 `test/e2e/suite/cases/m1-bootstrap.test.ts`**

替换 Task 3 的最小版本。四条断言全部是宿主侧可观测的，**不需要探针，不碰生产代码**。

```ts
import { strict as assert } from "node:assert";
import { readFileSync, realpathSync } from "node:fs";
import { join } from "node:path";
import * as vscode from "vscode";
import { E2E_MODEL, EXTENSION_ID, findTestPiProcesses } from "../../fixture.ts";
import { env, filesUnder, repoRoot, sessionsDir, waitFor, workspaceDir } from "../harness.ts";

interface Manifest {
  name?: string;
  publisher?: string;
  contributes?: { commands?: { command: string }[] };
}

function manifest(): Manifest {
  return JSON.parse(readFileSync(join(repoRoot(), "package.json"), "utf8")) as Manifest;
}

describe("M1 — harness bootstrap", () => {
  it("propagates extensionTestsEnv into the extension host", () => {
    // Settles M1 open question #1. runTest.js applies extensionTestsEnv through
    // cp.spawn's env, so it must be visible here. realpathSync on both sides
    // because macOS resolves /var/folders/... to /private/var/folders/...
    const expected = realpathSync(workspaceDir());
    const actualFolders = vscode.workspace.workspaceFolders ?? [];
    assert.equal(actualFolders.length, 1, "the fixture workspace should be the only folder");
    assert.equal(realpathSync(actualFolders[0]?.uri.fsPath ?? ""), expected);
    assert.ok(sessionsDir().length > 0);
  });

  it("activates the extension under its manifest id", async () => {
    const { name, publisher } = manifest();
    assert.equal(`${publisher}.${name}`, EXTENSION_ID, "EXTENSION_ID drifted from package.json");

    const extension = vscode.extensions.getExtension(EXTENSION_ID);
    assert.ok(extension, `${EXTENSION_ID} is not present in this VS Code instance`);
    if (!extension.isActive) await extension.activate();
    assert.equal(extension.isActive, true);
  });

  it("registers every command declared in contributes.commands", async () => {
    // Read from package.json rather than a hardcoded list, so adding or removing
    // a command cannot silently leave this assertion stale.
    const declared = (manifest().contributes?.commands ?? []).map((entry) => entry.command);
    assert.ok(
      declared.length >= 11,
      `expected the 11 contributed commands, saw ${declared.length}`,
    );

    const registered = new Set(await vscode.commands.getCommands(true));
    assert.deepEqual(
      declared.filter((command) => !registered.has(command)),
      [],
    );
  });

  it("spawns the real pi subprocess with the fixture arguments", async () => {
    // `--model solar/qoder/qwen3.8-flash` arrives through pi-agent-chat.args,
    // so matching on it proves pi spawned, the fixture args reached it, and the
    // process is ours rather than one from a VS Code the user has open.
    //
    // openSidebarChat bails silently in three places (chat-sidebar.ts:257-267),
    // and waitForView() has a hardcoded 5s timeout (chat-sidebar.ts:65-81). On a
    // cold extension host the view can miss that window, leaving no pi process
    // and no error — so re-issue the command until one appears. The command is
    // idempotent once sidebarState.session exists.
    const deadline = Date.now() + 90_000;
    for (let attempt = 1; ; attempt += 1) {
      await vscode.commands.executeCommand("pi-agent-chat.openInSidebar");
      const spawned = await waitFor(() => findTestPiProcesses().length > 0, {
        timeoutMs: 15_000,
        what: `a pi process carrying --model ${E2E_MODEL}`,
      })
        .then(() => true)
        .catch(() => false);
      if (spawned) return;
      if (Date.now() > deadline) {
        assert.fail(
          `no pi process carrying --model ${E2E_MODEL} after ${attempt} openInSidebar ` +
            "attempts. Likely causes: ensurePiBinary returned undefined because " +
            "pi-agent-chat.path is not executable (src/services/rpc/process.ts:55-71 " +
            "fails silently), or the sidebar view never resolved. The runner keeps the " +
            "fixture on failure — grep the rpcTrace output under " +
            "<fixture>/user-data/logs/*/window*/output_logging_*/ for the spawn record.",
        );
      }
      console.log(`[m1] openInSidebar attempt ${attempt} produced no pi process, retrying`);
    }
  });

  it("redirects pi session state into the fixture directory", async () => {
    // Recorded as a finding rather than a hard requirement: pi may not write a
    // session file until a message exists, which M1 never sends. If files do
    // appear, PI_CODING_AGENT_SESSION_DIR demonstrably reached the subprocess;
    // if not, M2 must decide whether to promote this to a hard assertion.
    const appeared = await waitFor(() => filesUnder(sessionsDir()).length > 0, {
      timeoutMs: 30_000,
      what: "a file under PI_CODING_AGENT_SESSION_DIR",
    })
      .then(() => true)
      .catch(() => false);

    console.log(
      `[m1 finding] session files under ${sessionsDir()}: ` +
        `${appeared ? filesUnder(sessionsDir()).join(", ") : "(none within 30s)"}`,
    );
    assert.ok(env("PI_E2E_SESSIONS").length > 0);
  });

  it("can read the pi binary the fixture pinned", () => {
    // ensurePiBinary (src/services/rpc/process.ts:55-71) silently returns
    // undefined when the binary is not executable, and openChatPanel then bails
    // without throwing. Assert the pin is sane so a silent no-op cannot be
    // mistaken for a passing run.
    const piBin = env("PI_E2E_PI_BIN");
    assert.ok(piBin.startsWith("/"), `expected an absolute pi path, got ${piBin}`);
    assert.notEqual(piBin, "", "pi-agent-chat.path must not be empty");
  });
});
```

- [ ] **Step 3: 重新打包**

Run: `pnpm run build:e2e`
Expected: 成功。

- [ ] **Step 4: 第一次真跑**

Run: `pnpm run test:e2e`
Expected: 构建 webview 与扩展 → 启动一个**新的 VS Code 窗口**（独立 user-data-dir，不影响你日常的 VS Code）→ mocha 报告 6 条用例全绿 → 窗口关闭 → 进程退出码 0，且打印 `[e2e] fixture   /var/folders/.../pi-e2e-XXXX`。

- [ ] **Step 5: 若失败，按这个顺序诊断**

1. **窗口没起来 / 立刻退出** → 检查 `[e2e] vscode` 那行打印的路径是否存在；用 `PI_E2E_VSCODE=<path> pnpm test:e2e` 显式指定
2. **`PI_E2E_REPO is missing from the extension host environment`** → `extensionTestsEnv` 没传播，M1 开放问题 #1 的答案是「否」；改用从 `vscode.workspace.workspaceFolders` 反推路径，并把这个结论写进 FINDINGS.md
3. **扩展未激活 / `EXTENSION_ID is not present`** → `--extensions-dir` 指空目录时开发扩展没加载；确认 `dist/extension.cjs` 存在（即 `pnpm build` 真的跑过），并试去掉 `--new-window`
4. **`timed out ... waiting for a pi process`** → 打开 fixture 里 `pi-agent-chat.rpcTrace` 已启用的输出通道 "Pi Chat RPC" 看有没有 spawn 记录；然后**用 `--no-offline` 重跑一次**以判断是不是 `PI_OFFLINE=1` 让 pi 启动即退（这正是 M1 开放问题 #2）。命令：`node test/e2e/.build/runner.cjs --no-offline --skip-build`
5. **卡在 workspace trust 弹窗** → `--disable-workspace-trust` 不够，需在 fixture settings 里加 `"security.workspace.trust.enabled": false`（M1 开放问题 #3）

调试时加 `--skip-build` 省掉每次 20s+ 的构建：`node test/e2e/.build/runner.cjs --skip-build`

- [ ] **Step 6: 连跑三次确认不 flake**

Run:

```bash
for i in 1 2 3; do echo "=== run $i ==="; pnpm run test:e2e --skip-build || exit 1; done
```

（若 `--skip-build` 没能透传，改用 `for i in 1 2 3; do node test/e2e/.build/runner.cjs --skip-build || exit 1; done`）
Expected: 三次都退出码 0。

- [ ] **Step 7: 提交（先问用户）**

```bash
git add test/e2e/runner.ts test/e2e/suite/cases/m1-bootstrap.test.ts
git commit -m "test(e2e): launch an isolated VS Code and assert the harness bootstraps"
```

---

### Task 5: 结论落档与文档

**Files:**

- Create: `test/e2e/FINDINGS.md`
- Modify: `README.md`（`Development` 段落，约在 100-139 行）

**Interfaces:**

- Consumes: Task 4 的实际运行结果
- Produces: M2 计划的输入

- [ ] **Step 1: 写 `test/e2e/FINDINGS.md`**

规格书 §4 M1 要求「上述不确定项全部有书面结论」。逐项填实际观察到的结果，**不要填推测**。模板：

```markdown
# M1 findings

规格书 §4 要求 M1 关闭下列不确定项。结论来自实际运行，不是推测。

## 1. `extensionTestsEnv` 是否传到扩展宿主

**结论：** <是 / 否>
**证据：** <用例 "propagates extensionTestsEnv into the extension host" 的通过或失败；
若失败，harness.ts 的 `env()` 抛出的诊断信息里 PI*E2E*\* 键的实际列表>
**对 M2–M4 的影响：** <尤其决定 M4 能否用 HOME 覆盖做凭据隔离（规格书 §5.1）>

## 2. `PI_OFFLINE=1` 会不会挡掉 LLM 请求

**结论：** <不影响启动 / 导致 pi 启动即退 / 未确定>
**证据：** <`node test/e2e/.build/runner.cjs --no-offline` 与默认运行的对比结果>
**对 M2 的影响：** <M2 要发真 prompt，若 PI_OFFLINE 挡请求则必须去掉>

## 3. `--disable-workspace-trust` 是否足够

**结论：** <足够 / 不够，需加 security.workspace.trust.enabled=false>
**证据：** <是否出现过 trust 弹窗阻塞>

## 4. 复用本机 VS Code 1.138.0 + 空 `--extensions-dir` 能否加载开发扩展

**结论：** <能 / 不能>
**证据：** <`[e2e] vscode` 打印的路径 + 扩展激活用例结果>

## 5. pnpm `onlyBuiltDependencies` 是否拦掉 postinstall 下载

**结论：** 不适用 —— `@vscode/test-electron@3.1.0` 的 package.json `scripts` 里没有
`install` / `postinstall`，安装期不下载 VS Code。下载只发生在 `runTests()` 未提供
`vscodeExecutablePath` 时。
**证据：** Task 1 Step 2 的命令输出。

## 6. pi 是否在无 prompt 时就写会话文件

**结论：** <是 / 否>
**证据：** 用例 "redirects pi session state into the fixture directory" 打印的
`[m1 finding]` 行。
**对 M2 的影响：** 若为「是」，M2 可把它升级为硬断言；若为「否」，M2 需先发一个
最小 prompt 才能用会话文件做断言。

## 7. 实测耗时

**一次完整 `pnpm test:e2e`（含 build）：** <秒>
**不含 build（`--skip-build`）：** <秒>
**其中 pi 进程出现在 `ps` 里的耗时：** <秒>
```

- [ ] **Step 2: 在 README 的 Development 段落追加 e2e 说明**

找到 `README.md` 的 `## Development` 段落（约 100-139 行），在现有命令列表之后追加：

````markdown
### End-to-end tests

`pnpm test:e2e` launches a **separate** VS Code instance against a throwaway
fixture workspace and drives the real `pi` binary. It never touches your daily
VS Code profile, extensions or sign-in state (`--user-data-dir` and
`--extensions-dir` both point into a temp directory), and pi's session storage
is redirected away from `~/.pi/agent` via `PI_CODING_AGENT_SESSION_DIR`.

Not part of `pnpm test` — e2e runs are local-only and need real pi credentials.

```sh
pnpm test:e2e                              # build, then run
node test/e2e/.build/runner.cjs --skip-build   # iterate without rebuilding
node test/e2e/.build/runner.cjs --no-offline   # let pi do startup network calls
```
````

Environment overrides:

| Variable        | Purpose                      | Default                                                 |
| --------------- | ---------------------------- | ------------------------------------------------------- |
| `PI_E2E_VSCODE` | VS Code executable to launch | the macOS install, else a download into `.vscode-test/` |
| `PI_E2E_PI`     | pi binary path               | `which pi`, else `~/.local/bin/pi`                      |

The model is pinned to `solar/qoder/qwen3.8-flash` in `test/e2e/fixture.ts`.
A trivial turn on that model takes ~20s, so case timeouts are 120s.

On success the fixture directory is deleted; on failure it is kept and its path
printed, because the `user-data-dir` and the "Pi Chat RPC" output channel are
the only way to diagnose a launch that never reported.

````

- [ ] **Step 3: 全量验证**

Run: `pnpm run lint && pnpm run typecheck && pnpm run test:unit`
Expected: 全绿。注意 `test:unit` 现在应包含 `test/e2e/fixture.test.ts`，但**不包含** `test/e2e/suite/**`。

Run: `pnpm run test:e2e`
Expected: 退出码 0。

- [ ] **Step 4: 确认没有残留**

Run:
```bash
ps -Awwo command= | grep -F -- "--model solar/qoder/qwen3.8-flash" | grep -v grep
ls -d /var/folders/*/*/T/pi-e2e-* 2>/dev/null
````

Expected: 两条都**无输出**（成功运行会删除 fixture；孤儿 pi 检查会让 runner 返回 1）。

- [ ] **Step 5: 提交（先问用户）**

```bash
git add test/e2e/FINDINGS.md README.md
git commit -m "docs(e2e): record M1 findings and document the harness"
```

---

## Self-Review

**规格书覆盖检查（仅 M1 范围）：**

| 规格书要求                                                       | 对应 Task                                                                                                                |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| §3.1 目录布局                                                    | Task 2/3/4 的 Files 段                                                                                                   |
| §3.2 障碍①：rolldown 打 CJS                                      | Task 3 Step 1、7、8                                                                                                      |
| §3.3 障碍②：`PI_CODING_AGENT_SESSION_DIR` 经 `pi-agent-chat.env` | Task 2 Step 3 `fixtureSettings`；Task 4 Step 1 `extensionTestsEnv` 双保险                                                |
| §3.3 `path`/`args` 必须激活前就位                                | Task 2 Step 3 注释 + Step 1 断言                                                                                         |
| §3.4 M2 才用探针 → M1 不碰生产代码                               | 全计划无 `src/**` 改动；Global Constraints 明列                                                                          |
| §3.5 启动配置                                                    | Task 4 Step 1                                                                                                            |
| §3.6 时序预算（120s / 复用会话 / 单次启动）                      | Task 3 Step 4 `SUITE_TIMEOUT_MS`；Task 4 Step 2 用例共用一次启动                                                         |
| §4 M1 用例 1（扩展激活）                                         | Task 4 Step 2 用例 2                                                                                                     |
| §4 M1 用例 2（11 个命令注册）                                    | Task 4 Step 2 用例 3（改为从 manifest 读，防漂移）                                                                       |
| §4 M1 用例 3（侧边栏可见）                                       | Task 4 Step 2 用例 4 —— 用 `openInSidebar` + pi 进程出现作为可观测信号，因为 VS Code 没有查询 view visibility 的公开 API |
| §4 M1 用例 4（pi 子进程起来）                                    | Task 4 Step 2 用例 4（`ps` 匹配 `--model`，比会话文件更可靠）；用例 5 把会话文件降级为 finding                           |
| §4 M1 用例 5（无孤儿进程）                                       | Task 4 Step 1 `findTestPiProcesses` 收尾检查 + Task 5 Step 4                                                             |
| §4 M1 五项不确定项                                               | Task 1 Step 2（#5 已关闭）、Task 5 Step 1（#1–#4 + #6 落档）                                                             |
| §4 M1 通过标准                                                   | Task 4 Step 6（连跑 3 次）、Task 5 Step 3–4                                                                              |
| §6 风险「pi 静默失败」                                           | Task 4 Step 2 用例 6（主动断言 `PI_E2E_PI_BIN` 合法）                                                                    |
| §6 风险「孤儿进程 / 临时目录残留」                               | Task 4 Step 1（失败时保留、成功时删除）+ Task 5 Step 4                                                                   |

**规格书偏离（有意，已在上表标注理由）：**

1. M1 用例 4 的证据从「tmpdir 里出现会话文件」改为「`ps` 匹配 `--model solar/qoder/qwen3.8-flash`」。理由：pi 可能等有消息才落盘，而 M1 不发 prompt；`--model` 由我们通过 `pi-agent-chat.args` 注入，唯一且精确，还能区分测试的 pi 与用户日常的 pi。会话文件降级为 FINDINGS.md 第 6 项，由 M2 决定是否升级为硬断言。
2. 命令注册断言从硬编码 11 个名字改为运行时读 `package.json` 的 `contributes.commands`。理由：命令增删时不会悄悄失效。保留 `>= 11` 的下限断言以防 manifest 读取本身失败。
3. 新增 `test/e2e/fixture.test.ts`（vitest 单测）。理由：fixture 的路径与 settings 生成逻辑最容易写错，而每次靠启动 VS Code 验证要 20s+；单测让它进 `pnpm test:unit`，秒级反馈。这是规格书未提的补充，不改变任何已定决策。
4. 用例 4 从「执行一次 `openInSidebar` 然后等 90s」改为「重试循环 + 可操作的失败信息」。理由：执行期复查发现 `openSidebarChat` 有三处静默 return（`chat-sidebar.ts:257-267`），且 `waitForView()` 超时硬编码 5000ms（`chat-sidebar.ts:65-81`）；冷启动扩展宿主可能错过这 5 秒窗口，导致 pi 不 spawn 且不报错。重试是安全的（`sidebarState.session` 存在时命令幂等），失败信息改为直接指向 `ensurePiBinary` 的静默失败路径与 rpcTrace 日志位置。

**占位符扫描：** 无 TBD / TODO / 「类似 Task N」。`runner.ts` 与 `m1-bootstrap.test.ts` 在 Task 3 是**明确标注的最小占位**并在 Task 4 被完整替换，替换后的完整代码都写在 Task 4 里，不是留白。`FINDINGS.md` 模板里的 `<...>` 是待填的实测结果，其填写指令与证据来源在 Task 5 Step 1 中逐项指明。

**类型一致性核对：**

- `Fixture` 接口的 6 个字段在 Task 2 定义、Task 4 `runner.ts` 的 `createFixture` 返回值与 `runTests` 参数中逐字使用 ✓
- `findTestPiProcesses(lines?: string[])` 在 Task 2 定义，Task 4 两处调用都不传参（用默认值）✓
- `waitFor(predicate, { timeoutMs, intervalMs?, what? })` 在 Task 3 定义，Task 4 三处调用都传 `timeoutMs` + `what` ✓
- `env` / `repoRoot` / `workspaceDir` / `sessionsDir` / `filesUnder` 在 Task 3 定义，Task 4 用例的 import 列表与之完全一致 ✓
- `E2E_MODEL` / `EXTENSION_ID` 在 Task 2 定义，Task 4 从 `../../fixture.ts` 导入 ✓
- `SUITE_TIMEOUT_MS = 120_000` 与 Global Constraints 的「单个真 pi turn 120s」一致 ✓
- 产物路径 `test/e2e/.build/suite/index.cjs` 在 Task 1（`test:e2e` script）、Task 3（rolldown `entryFileNames`）、Task 4（`extensionTestsPath`）三处一致 ✓
