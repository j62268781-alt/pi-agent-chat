node_modules
*.log
.env*
dist
*.vsix
package-lock.json
.DS_Store
# the retired e2e harness's leftover build output (`test/unit` is tracked)
/test/e2e/.build/

# webview-vue build output
webview-vue/dist

# generated at build time
webview-vue/src/lib/model-icons-data.ts

# TypeScript native preview single-file check config
.tsconfig.pi-extensions-check.json
# transient commit message used by tooling
.commit-msg

# agent working notes (same convention as pi-agent-studio)
.workbuddy/
